# Production build (npm run build).

# Point at the production backend. Edit to match your deployment.
VITE_API_BASE_URL=https://flink.example.com/api

# Call the backend directly (no mocks, no dev proxy).
VITE_API_MODE=direct
