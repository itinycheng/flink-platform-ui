# Staging build (npm run build:stage).

# Point at the staging backend. Edit to match your deployment.
VITE_API_BASE_URL=https://flink-stage.example.com/api

# Call the backend directly (no mocks, no dev proxy).
VITE_API_MODE=direct
